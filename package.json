{
  "name": "tarsen",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*", "integrations/*"],
  "scripts": {
    "dev": "npm run dev --workspace @tarsen/web",
    "build": "npm run build --workspace @tarsen/core && npm run build --workspace @tarsen/cli && npm run build --workspace @tarsen/web",
    "test": "npm run test --workspaces --if-present",
    "package": "scripts/package"
  }
}
